
## Beginner

1. What is type inference in TypeScript?
2. What is the difference between type inference and type annotation?
3. What type does TypeScript infer for `const age = 25`?
4. Can TypeScript infer the type of arrays and objects?
5. Why shouldn't you explicitly annotate every variable?

## Intermediate

1. When should you use explicit typing instead of inference?
2. What type is inferred for `const skills = ["React", "TypeScript"]`?
3. Why does `let address: string | null = null` need an explicit type?
4. Can TypeScript infer the return type of a function?
5. What is the difference between `number[]` and `[number, number]`?

## Hard

1. How does TypeScript infer types from object properties?
2. What happens when a variable is declared without an initial value?
3. Why can `const user = {...}` still allow modification of `user.name`?
4. How do union types affect type inference?
5. When is explicit return typing useful even when TypeScript can infer the return type?

---
